// SPDX-License-Identifier: GPL-2.0-only
//! ModManager graphical frontend (`Iced`).
//!
//! The same feature set as the CLI and TUI, themed toward a clean macOS-like
//! look: drag-to-reorder load order, the FOMOD install wizard, settings, and an
//! optional tray/headless mode so browser clicks work while the window is
//! closed. Iced (MIT) is the only GUI toolkit this workspace links.
//!
//! Implemented in Phase 5; this is the binary skeleton.

fn main() {}
